# Steps to setup a development environment for the frontend

- Install Elm from <https://guide.elm-lang.org/install/elm.html> and follow the steps on the page.
- Install node package manager and run `npm install`
